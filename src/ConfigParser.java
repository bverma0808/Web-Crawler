import java.util.*;

public class ConfigParser {

	/**
	* parse a config file into the memory
	* the contents of the config file should be key value pairs
	* separated by equals sign "=". Comments should start with '#'
	* @param fileFullPath : path to the config file along with file name
	* @return if successful, then a Map containing the key value pairs of configs is returned
    *               otherwise null is returned
	*/
	public static HashMap<String,Object> parse(String fileFullPath){
        return null;
	}

}